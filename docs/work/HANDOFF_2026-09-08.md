<!-- Copyright 2026 Rhett Creighton. Licensed under Apache-2.0. -->

# Development handoff: Insight, Control, and publication throughput

This is a development checkpoint, not a new priority authority. Start with
[`../../AGENTS.md`](../../AGENTS.md), then the first open item in
[`FORWARD_PLAN.md`](./FORWARD_PLAN.md). The requested program is still
incomplete. Maintainer-host production state is outside this handoff.

## Outcome and implemented scope

The mission now describes the complete native Insight + Control journey,
with canonical lifecycle convergence first. Four commits through
`49660a8e0b60097fe83a1344bd7db4e926f428f5` were published and independently
observed on `origin/main` during this session:

- `04b9cd0c1`: refuse eligible contradictory signed proof observations;
  clarify input keys versus observation roots and the ordered program.
- `3d2a8e892`: preserve valid dependency-relative libtool links while refusing
  escapes, dangling links and loops; keep lint fixtures under ignored scratch.
- `123b3b49b`: proof fixtures use private production permissions, including
  an explicit refusal test for group-writable evidence.
- `49660a8e0`: declare `realpath` portability correctly and repair shifted
  flag-registry references.

The subsequent code commit is titled
"Serialize landing preparation with exact proof execution".
Queued exact proofs share the existing landing `step.lock` before claim and
through execution and cleanup. Preparation takes it exclusively. Contention
leaves requests intact instead of creating failed proof observations. Final
preparation reruns the native Make restart-plan target after prerequisites
settle; an existing stale plan cannot conceal Make failure. Edit-cycle workers
are outside this queue guard. Windows proof production remains unavailable.

## Evidence and verification

The conflict admission tests preserve signatures, eligibility, freshness,
revocation and package-result checks. They assert refusal before proof
materialization or trust promotion. This is same-action evidence admission;
cross-candidate proof reuse and full lifecycle convergence are not established.

Canonical focused tests, public C23 build, generated interfaces, flag and
portability gates, `lint-fast`, and non-author review passed for the scheduling
change. Its regressions first failed in both registered groups, then passed:

```text
/tmp/z23-landing-order-red.log       SOME TESTS FAILED, 2/2, 167.2 seconds
/tmp/z23-landing-order-green.log     ALL TESTS PASSED, 0/2, 81.0 seconds
/tmp/z23-landing-order-lint.log      lint-fast: OK
/tmp/z23-landing-order-build.log     c23-node: PASS
```

These are local logs, not portable evidence. Final publication must be checked
against the exact native receipt and remote identity. The commands below use
native queue status and the proof schema; use the recorded publication pair:

```bash
git fetch origin main
git status --short --branch
git rev-parse HEAD origin/main
git ls-remote origin refs/heads/main
build/bin/z23-dev dev land status
build/bin/z23-dev discover schema dev.proof.status
```

The default Linux landing root is beneath the operator's XDG state directory,
in the z23/dev/land/wt subdirectory. Its `.cache/zcl-dev-proof/receipts` directory retains
files named by exact commit and base. Pass those IDs explicitly as
`local_commit` and `remote_base` to `dev.proof.status`, with the correct `root`;
do not substitute the now-advanced remote tip. The earlier published
`49660a8e0` proof passed 32 selected test groups and all
landing lint gates, including strict Windows cross-link acceptance. That is
Linux development evidence, not native macOS or three-host release acceptance.

## Measured bottlenecks

For that exact commit against base `68ebfde1704e1be597e6e9e077ba239f486bccdf`,
one local proof used Linux x86-64, Ryzen 9 7950X3D, 32 logical CPUs, RAM scratch,
an existing warm donor, and a 32-worker test runner. Compiler and build inputs
are bound by its receipt. This was one development attempt, not a fleet-load
benchmark or p95 claim. Its phases recorded approximately 13 seconds for
impact closure, 35 seconds compiling, 82 seconds preparing shared tools, and
205 seconds for concurrent lint/test completion. Full lint alone took 191
seconds, above its 75-second soft budget. Before the scheduling fix, premature
proofs repeatedly failed while the landing step was still preparing the tree.

The attempt evidence is beneath the landing root's `.cache/zcl-dev-proof`.
Keep failed attempts. Repair prerequisites, then use explicit
`dev.proof.retry`; never delete failure markers or manufacture receipts.

One remaining scheduling limitation was observed: an obsolete foreground edit
cycle delayed the resident watcher from reaping a deferred queue worker and
draining its request. Restarting that exact development watcher after landing
preparation let the unchanged request proceed. The queue guard prevents the
premature proof; bounding foreground collection remains follow-up work.

The integrated warm proof for `4440e3bee` passed all 39 selected test groups
but failed the hardlink-isolation gate: warm seeding linked both rollback
fixture dependency files beneath build/hotswap. The generic warm classifier
admits dependency files there even though this dependency room requires
independent inodes. Preserve that failed attempt. The handoff publication uses
the existing `ZCL_DEV_PROOF_WARM=0` cold-proof mode with every gate enabled;
set it when starting the exact landing watcher. Repair warm classification
with an isolation regression before claiming this warm path is qualified.

## Next developer

1. Verify the checkpoint above and continue queue item 1 in
   [`FORWARD_PLAN.md`](./FORWARD_PLAN.md), using
   [`CANONICAL_LIFECYCLE.md`](./CANONICAL_LIFECYCLE.md) for the unfinished
   authority mapping. Native Git receipts are still a transitional format.
2. Finish actual input-closure keys and cross-candidate reuse, durable
   publication intent, expected-base/ancestry checks and independent recovery
   observation. A read-only audit identified `dl_already_landed` ignoring a
   fetch failure; add its stale-remote regression before changing behavior.
3. Optimize measured shared-tool and lint costs using exact reusable inputs.
   Keep mandatory evidence; do not infer speed from agent count or enqueue.

Fleet pagination/privacy, continuous telemetry, business ingestion, service
and traffic control, sync acceptance, and the complete three-host demonstration
remain program work. No production deployment, custody operation, consensus
change, remote signing or physical-host acceptance was performed here. Obtain
fresh scoped owner authority and qualify bidirectional routes before that work.
Preserve unrelated worktrees. Use typed development watcher status/stop/ensure
commands for an exact checkout; never kill every process named `z23-dev`.
