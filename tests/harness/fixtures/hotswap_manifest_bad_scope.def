/* Lint-gate fixture manifest: a forbidden consensus-root path. Used by the
 * check_hotswap_eligible_scope self-test to prove the gate trips (exit 1). */
HOTSWAP_ELIGIBLE("lib/consensus/src/pow.c") HOTSWAP_PROBE("fixture_probe")
