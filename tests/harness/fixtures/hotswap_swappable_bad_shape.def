/* Lint-gate fixture: a swappable row whose source_tu resolves under a forbidden
 * consensus root. Used by the check_hotswap_swappable_shape self-test to prove
 * the shape half of the hard line trips (exit 1). NOT a real allowlist. */
HOTSWAP_SWAPPABLE("lib/consensus/src/pow.c", "core.status")
