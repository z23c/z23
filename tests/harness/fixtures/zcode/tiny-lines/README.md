# tiny-lines

A deliberately small C23 fixture for measuring the ZCODE development loop.
It counts logical lines in a caller-owned byte span without allocation.

This package exists as stable test material; it is not linked into the node.
