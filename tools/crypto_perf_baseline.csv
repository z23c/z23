# zclassic23 crypto-vs-rust performance baseline.
#
# The STANDING invariant (docs/CRYPTO_PERF.md): every C crypto primitive on the
# consensus path must stay AT LEAST as fast as its Rust/blst counterpart, and
# may only ever get FASTER. This file is the pinned line. It is consumed by
# tools/scripts/check_crypto_perf.sh (`make check-crypto-perf`), which measures
# the live C median (build/bin/zclassic23 -bench-crypto-vs-rust) and enforces:
#
#   RATCHET  (always, hard):  measured_c_ns <= c_ns_baseline * (1 + margin).
#            c_ns_baseline is a CEILING — it may only shrink as we optimise.
#            A self-regression FAILS. margin default 0.20 (see the script).
#
#   RATIO    (vs Rust):       ratio = measured_c_ns / rust_ns_baseline.
#            gate_mode=beat   -> hard-FAIL if measured_c_ns >= rust_ns_baseline
#                                (we must stay ahead). Only valid when
#                                rust_ns_baseline >= c_ns_baseline*(1+margin),
#                                which makes the beat hard-fail FLAKE-PROOF:
#                                any run that passes the ratchet is < rust.
#            gate_mode=behind -> NO hard fail (would red main today); prints a
#                                loud "BEHIND RUST — optimize" line and relies
#                                on the ratchet for monotonic improvement.
#
# Columns:
#   primitive          key emitted by `-bench-crypto-vs-rust` (CRYPTOPERF <key>)
#   c_ns_baseline      pinned C ns/op ceiling (lower-is-better)
#   rust_ns_baseline   pinned Rust/blst/reference ns/op
#   gate_mode          beat | behind
#   rust_source        MEASURED | CITED  (+ where the Rust number comes from)
#   notes              provenance of the C baseline
#
# C baselines measured 2026-07-13 on AMD Ryzen 9 7950X3D (this host), pure-C23
# build (-march=x86-64-v3, no SHA-NI, no hand asm), median of 5 x 120ms samples,
# pinned slightly above the max observed across runs for ratchet headroom.
# Rust numbers are CITED published reference points (blst / RustCrypto /
# librustzcash / zcashd) on comparable modern x86-64 unless labelled MEASURED —
# NO Rust is linked into the shipped binary (no-external-deps rule); this is a
# bench-only comparison. Re-baseline (shrink only) on this host as we optimise.
primitive,c_ns_baseline,rust_ns_baseline,gate_mode,rust_source,notes
equihash-200-9,128000,160000,beat,"CITED zcashd C++ CheckEquihashSolution ~150-175us (no Rust equihash exists; librustzcash predates it)","check_equihash_solution on baked real 200,9 witness; obs 115.6-126.1us"
bls12-381-fp-mul,37,45,beat,"CITED blst mul_fp ~40-45ns (hand asm mulx/adcx/adox)","fp_mul on canonical Fp fixtures; obs 31.0-31.5ns over 3 runs after the real ADCX/ADOX dual carry chain (core/modules/sapling/src/mont_adx.h) replaced the MULX-only path that was slower than portable C"
bls12-381-pairing,1900000,600000,behind,"CITED blst miller_loop+final_exp single pairing ~0.5-0.65ms","bls12_381_pairing on canonical G1/G2 generators; obs 1.79-1.87ms"
secp256k1-ecdsa-verify,50000,90000,beat,"CITED RustCrypto k256 ecdsa verify ~85-95us (libsecp256k1 C bindings match ours)","pubkey_verify on locally-signed 32-byte hash; obs 47.9-49.1us (vendored libsecp256k1)"
ed25519-verify,850000,55000,behind,"CITED ed25519-dalek verify ~50-60us (wNAF + precomputed tables)","ed25519_verify on RFC 8032 Test 2; obs 662-813us (median 710us over 9 runs) after the verify equation became ONE interleaved Straus double-scalar multiply sharing its doublings, replacing two independent cswap ladders. Ceiling shrunk from 1500000 (median 1496us over 5 runs on the same host and build immediately before the change = 2.11x). Still BEHIND: dalek also has a precomputed base-point table and a 5x51-limb field, neither of which this has — the field is still TweetNaCl 16x16, which is the next and much larger win"
sha256,560,680,beat,"CITED RustCrypto sha2 w/ SHA-NI ~1.5-2GB/s (~512-680ns/1KiB)","sha256 over 1 KiB; SHA-NI reachable on -v3 since the #ifdef __SHA__ removal (runtime dispatch); obs 466.6-480.2ns = ~2.1GB/s over 6 runs. Pinned 17% over max-observed (not the usual ~2%) because the number came from an isolated single-TU harness at -march=x86-64-v3, not from -bench-crypto-vs-rust on the linked binary. Assumes a SHA-NI host, as the AVX2 blake2b row already does; on a non-SHA-NI host this falls back to ~1950ns and trips the ratchet by design"
sha3-256,1800,2048,behind,"CITED RustCrypto sha3 keccak scalar ~0.5GB/s (~2048ns/1KiB)","zcl_sha3_256 over 1 KiB; obs 1.71-1.78us = ~0.58GB/s (slim; establish margin)"
blake2b,870,1100,beat,"CITED blake2b scalar reference ~0.9GB/s (~1100ns/1KiB)","blake2b-512 over 1 KiB; obs 830-862ns = ~1.2GB/s (AVX2 path)"
groth16-bls12-381-output,7950000,3000000,behind,"CITED librustzcash single Sapling output proof verify ~2-4ms","sapling_check_output full BLS12-381 pairing on real prover proof; obs 7.56-7.92ms (BEHIND — primary optimize target)"
