# check_proc_self_shim.sh baseline — grandfathered raw /proc/self or
# /proc/uptime reads outside lib/platform/. RATCHET, shrink-only: a file
# here may be removed once migrated onto platform/os_proc.h; no new file
# may be added. lib/sim/src/postmortem.c:1040 is the one PERMANENT
# exemption (async-signal-safe crash handler — the shim is not
# signal-safe) rather than a migration debt; see its in-code comment.
config/src/consensus_state_snapshot_export_fd_io.c
config/src/consensus_state_snapshot_candidate_output.c
config/src/consensus_state_snapshot_export.c
config/src/consensus_state_snapshot_output_seal.c
# consensus_state_producer_receipt.c hashes /proc/self/exe through an open file
# descriptor so the receipt names the executing bytes without a path race.
config/src/consensus_state_producer_receipt.c

config/src/consensus_state_snapshot_export_proof.c
lib/util/src/thread_profile.c
lib/sim/src/postmortem.c
lib/test/src/test_replay_canary_verdict.c
lib/test/src/test_make_lint_gates.c
lib/test/src/test_consensus_state_snapshot_export.c
lib/metrics/src/metrics.c
lib/hotswap/src/hotswap_activate.c
lib/hotswap/src/hotswap_loader.c
lib/test/src/test_wallet_persistence_cycle.c
# Reconciled at gate-landing (files predate the gate on main; the lane forked
# earlier). The next four are PERMANENT-exemption class, not migration debt:
# direct open("/proc/self/exe") is the race-free way to hash the running
# binary (a path readlink reintroduces TOCTOU), and "/proc/self/fd/N/…" is
# capability-relative path construction (openat semantics), not a proc read.
# lib/util/src/authority_receipt.c generalizes exactly that race-free
# running-binary digest (PERMANENT-exemption class, same reason).
lib/util/src/authority_receipt.c
app/services/src/canary_sentinel_watch.c
config/src/consensus_state_replay_receipt.c
app/controllers/src/anchor_controller.c
lib/test/src/test_canary_sentinel_watch.c
config/src/consensus_state_snapshot_install_activate.c
lib/storage/src/progress_store.c
lib/storage/src/projection_store.c
